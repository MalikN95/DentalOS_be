export enum AppointmentStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  ARRIVED = 'arrived',
  IN_TREATMENT = 'in_treatment',
  COMPLETED = 'completed',
  NO_SHOW = 'no_show',
}
